const theTruth = function() {
  return true;
}

const sayHey = function() {
  return 'Hey!';
}

const sayHeyFriend = function(name) {
  return `Hey, ${name}!`;
}

const favoriteIceCream = function(flavor) {
  return `I love ${flavor}`;
}

const shouting = function(phrase) {
  return phrase.toUpperCase();
}

const roundDown = function(number) {
  return Math.floor(number);
}
