const theTruth = () => (true);

const sayHey = () => ('Hey!');

const sayHeyFriend = (friend) => (`Hey, ${friend}!`);

const favoriteIceCream = (iceCream) => (`I love ${iceCream}`);

const shouting = (str) => (str.toUpperCase());

const roundDown = (num) => (Math.floor(num));
